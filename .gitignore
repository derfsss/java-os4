# Build output / release artefacts
/build/
/dist/

# Vendored upstream source trees (cloned/fetched, not committed here).
# Track only our patches under docs/ / vendor patches if/when they exist.
/vendor/jamvm/
/vendor/icedtea8/
/vendor/fallback/
/vendor/*.tar.*
/vendor/*.zip

# Editor / OS cruft
*.o
*.d
*.a
*.swp
*~
.DS_Store

# Java build artefacts
*.class

# Python bytecode
__pycache__/
*.pyc

# AI-assistant / internal working docs
CLAUDE.md

# Internal plans + working notes (planning / scratch docs -- never pushed)
/PLAN.md
PLAN*.md
/plans/
*.plan.md
plan-*.md
/docs/revival-notes.md

# The OpenJDK 17 (Zero) spike lives on the jdk17 branch, not on main.
/jdk17/
